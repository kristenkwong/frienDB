extends layout

block content
  h1= title
  p Welcome to #[em frienDB],  a very basic Social Media Networking Database made with Express and (currently) MongoDB

  h1 Site Statistics

  if error
    p Error getting dynamic content.
  else
    p This database has the following records:

    ul
      li #[strong Users:] !{data.user_count}
      li #[strong Tags:] !{data.tag_count}
      li #[strong Posts:] !{data.post_count}
